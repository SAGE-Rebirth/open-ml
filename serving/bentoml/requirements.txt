# BentoML's `fs` dep imports pkg_resources; Python 3.12 slim doesn't ship it,
# and setuptools>=81 REMOVED it — so pin an older setuptools that still has it.
setuptools<81
bentoml==1.3.20
mlflow==2.19.0
scikit-learn==1.5.2
boto3
pandas
numpy
