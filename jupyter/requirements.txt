# OpenML JupyterLab — client libraries.
# Base image (scipy-notebook) already provides numpy, pandas, scikit-learn,
# matplotlib, scipy, seaborn, jupyterlab. We add the ML/MLOps clients here.

# storage / data
boto3==1.35.90
s3fs==2024.12.0
minio==7.2.12
pyarrow==18.1.0
dvc[s3]==3.59.0
# pin: pathspec 1.x drops the internal _DIR_MARK symbol that dvc 3.59 imports,
# which breaks `dvc init/add`. 0.12.x is the last compatible line.
pathspec==0.12.1

# experiment tracking (client used from notebooks; servers live in later phases)
mlflow==2.19.0

# deep learning (CPU wheels on arm64; GPU-heavy work runs natively on host)
torch==2.5.1
torchvision==0.20.1

# vision
opencv-python-headless==4.10.0.84

# telemetry / messaging
prometheus-client==0.21.1
redis==5.2.1
aim==3.25.0

# orchestration (Phase 3) — pinned to match the zenml-server image tag.
# Core coexists cleanly with mlflow/dvc/aim; these two are the only extra deps
# the mlflow + s3 integrations need (our mlflow/s3fs/boto3 already satisfy the rest).
zenml==0.96.3
python-rapidjson<1.15
aws-profile-manager
